import { MaleorfemalePipe } from './maleorfemale.pipe';

describe('MaleorfemalePipe', () => {
  it('create an instance', () => {
    const pipe = new MaleorfemalePipe();
    expect(pipe).toBeTruthy();
  });
});
