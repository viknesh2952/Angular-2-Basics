import { ExdirectiveDirective } from './exdirective.directive';

describe('ExdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ExdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
